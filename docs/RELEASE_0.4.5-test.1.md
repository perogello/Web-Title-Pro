# Web Title Pro 0.4.5-test.1

## RU

Тестовый prerelease для полного smoke-теста встроенного updater.

- Добавлена видимая тестовая плашка в Settings -> System.
- Используется updater из `0.4.4` с проверкой полного размера скачанного `.exe`.
- Релиз предназначен только для проверки сценария `0.4.4 -> 0.4.5-test.1`.

## EN

Test prerelease for an end-to-end built-in updater smoke test.

- Adds a visible test banner in Settings -> System.
- Uses the `0.4.4` updater with full downloaded `.exe` size validation.
- Intended only for validating the `0.4.4 -> 0.4.5-test.1` update path.
