# Web Title Pro 0.2.6

## RU

- Исправлен file-based рендер пользовательских и системных шрифтов в локальных титрах
- Упрощена логика style editor: выбор шрифта теперь опирается на конкретный файл шрифта
- Проведена дополнительная чистка шрифтовой цепочки и релизной сборки

Рекомендуемый файл:
- `WebTitlePro.exe`

## EN

- Fixed file-based rendering for user-installed and system fonts in local titles
- Simplified the style editor logic so font selection now relies on the actual font file
- Performed additional cleanup across the font pipeline and release build

Recommended file:
- `WebTitlePro.exe`
