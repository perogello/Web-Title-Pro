## RU

- Критически исправлен portable updater flow
- Обновление теперь заменяет правильный `WebTitlePro.exe`, а не временный внутренний `exe`
- Улучшено окно статуса обновления: оно больше не должно зависать без понятного завершения
- Исправлен краш интерфейса при напоминании таймера

Рекомендуемый файл:
- `WebTitlePro.exe`

---

## EN

- Critically fixed the portable updater flow
- Updates now replace the correct `WebTitlePro.exe` instead of a temporary internal executable
- Improved the update status window so it no longer hangs without a clear finish state
- Fixed the timer reminder UI crash

Recommended file:
- `WebTitlePro.exe`
