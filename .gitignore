node_modules/
dist/
release/
storage/
.tmp-test/
tmp-verify/
test-results/
*.log
desktop-*.log
Thumbs.db
.DS_Store
templates/*.zip
templates/*.7z
/.claude/

# Runtime state — contains operator's real data (vMix URLs, entry names,
# tokens). Must NEVER be in the public repo. The runtime writes a fresh
# copy under userData/data/state.json on first launch.
data/state.json


